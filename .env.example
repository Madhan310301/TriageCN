# TriageCN Environment Configuration Example
# TriageCN is a 100% client-side, zero-backend, zero-external-API static application.
# All data (540 CVEs, organization profiles, gold-set validation records) is bundled at build time.
PORT=5173
NODE_ENV=production
